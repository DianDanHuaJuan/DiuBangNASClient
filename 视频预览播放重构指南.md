# 视频预览播放重构指南

> 基于 progressive + HTTP Range 方案重构局域网视频预览播放功能，并新增倍速播放、双击前进 5 秒、长按 2 倍速播放。

- 客户端项目：`d:\tmp\DiuBangNASClient`
- 安卓服务端：`D:\tmp\diubangNASServer_Android`
- Windows 服务端：`D:\tmp\diubangNASServer_Windows`

---

## 一、背景与目标

### 1.1 现存问题

当前视频预览在 HLS 流式模式下存在三类问题：

1. **时间轴长度识别不正确**
   - 服务端 FFmpeg 使用 `event` 类型 playlist，编码完成前不写 `#EXT-X-ENDLIST`，ExoPlayer 报告的 duration 是"已编码部分时长"而非视频真实时长。
   - 客户端通过直接篡改 `controller.value.copyWith(duration: ...)` 来覆盖，导致平台层状态与对外报告不一致，引发进度条/seek 状态机混乱。

2. **进度条拖动卡顿**
   - seek 超出编码前沿时走"杀进程 + 重启 FFmpeg + 重新加载 m3u8"流程，延迟达秒级到十秒级。
   - 客户端因 Chewie 进度条无 `onDragEnd` 回调，只能靠监听 position 跳变"猜 seek"，阈值难调，易误判或漏判。
   - **安卓服务端未注册 `/api/v1/preview/hls/seek` 路由**（`api_router.dart` 仅有 manifest 与 asset 两个 HLS 路由），客户端的 seek-restart 调用必然 404。

3. **客户端补偿逻辑过度复杂**
   - 约 250 行代码（11 个方法 + 8 个状态字段）专门用于弥补服务端 seek 能力缺失，维护成本高。

### 1.2 重构目标

- 采用 **progressive + HTTP Range** 作为局域网视频播放的唯一主路径，让 ExoPlayer 原生处理 duration 与 seek。
- 删除全部 HLS 时间轴补偿逻辑，恢复平台状态一致性。
- 新增三项交互功能：倍速播放、双击前进 5 秒、长按 2 倍速。

---

## 二、现状诊断

### 2.1 服务端 Range 支持（关键基础，已就绪 ✅）

经核实，**安卓与 Windows 服务端的 WebDAV 层均已完整支持 HTTP Range**，两端实现一致：

| 能力 | 文件 | 现状 |
|------|------|------|
| HEAD 返回 `Accept-Ranges: bytes` + `Content-Length` | `features/webdav/handlers/head_handler.dart` | ✅ 已支持 |
| GET 解析 `Range` 头 | `features/webdav/handlers/get_handler.dart` | ✅ 已支持 |
| Range 头解析（含 suffix range） | `features/webdav/utils/range_parser.dart` | ✅ 已支持 |
| 范围读取 `file.openRead(start, end)` | `features/webdav/readers/file_system_content_reader.dart` | ✅ 已支持 |
| 响应头带 `Accept-Ranges: bytes` / `Content-Range` | `features/webdav/utils/dav_response_header_builder.dart` | ✅ 已支持 |

**结论：progressive + Range 方案的服务端基础设施完全就绪，本次重构无需改动服务端 WebDAV 层。**

### 2.2 服务端预览策略（默认 progressive ✅）

两端 `features/api/handlers/preview_handler.dart` 默认配置一致：

```
progressiveVideoPreviewEnabled = true   ← 默认开启
hlsVideoPreviewEnabled = false          ← 默认关闭
transcodeVideoPreviewEnabled = false    ← 默认关闭
```

默认所有视频走 `progressive`，url 指向 `/dav/{path}`。HLS 路径默认未启用。

### 2.3 服务端 HLS seek 能力不一致（仅影响可选后备）

| 端 | seekSession 实现 | FFmpeg `-ss` | seek 路由 |
|----|:---:|:---:|:---:|
| Windows | ✅ `seekSession(seekOffsetMs)` | ✅ 有 | ✅ 已注册 |
| 安卓 | ❌ 无 | ❌ 无 | ❌ 未注册 |

Windows 服务端的 `video_hls_session_service.dart` 是更完善版本（带 `-ss` 快速 seek、`temp_file` 防止 serve 半成品 segment）。安卓服务端为旧版本。**本次主方案不依赖 HLS，此差异不影响重构；若未来启用 HLS 后备，需将安卓服务端对齐 Windows 版本（见附录）。**

### 2.4 客户端播放器能力

- 播放器：`video_player: ^2.11.1` + 自定义 fork `third_party/extended_video_player_android`
- 底层引擎：Android ExoPlayer
- 倍速：`VideoPlayerController.setPlaybackSpeed(double)` 为标准 API，fork 已实现原生通道 `setPlaybackSpeed(textureId, speed)`，**无需改动原生层**。
- 控件：自建 UI（`_buildControlsOverlay`），仅复用 chewie 的 `MaterialVideoProgressBar`，便于扩展。

---

## 三、方案总览

### 3.1 核心思路

```
┌─────────────────────────────────────────────────────┐
│  客户端 VideoPlayerController.networkUrl(/dav/path) │
│                         │                            │
│          ExoPlayer 原生处理（不篡改 value）          │
│         ├─ duration：从容器元数据读取（准确）        │
│         ├─ seek：发 HTTP Range 请求（毫秒级）        │
│         └─ 倍速：setPlaybackSpeed（原生支持）        │
└─────────────────────────────────────────────────────┘
                          │ HTTP Range
┌─────────────────────────▼────────────────────────────┐
│  服务端 WebDAV GET /dav/{path}                       │
│  Range: bytes=1000-2000 → 206 Partial Content        │
│  （已就绪，无需改动）                                 │
└──────────────────────────────────────────────────────┘
```

### 3.2 重构范围

| 模块 | 改动 | 说明 |
|------|------|------|
| 服务端 WebDAV | 无 | Range 已支持 |
| 服务端 preview meta | 无 | 默认 progressive |
| 服务端 HLS | 无（主方案不依赖） | 可选后备见附录 |
| 客户端 `video_preview_view.dart` | **主要改动** | 删除 HLS 逻辑 + 新增三功能 |
| 客户端原生层 | 无 | setPlaybackSpeed 已就绪 |

---

## 四、客户端重构：删除 HLS 补偿逻辑

> 文件：`d:\tmp\DiuBangNASClient\lib\features\preview\presentation\widgets\video_preview_view.dart`
>
> ⚠️ 以下行号基于当前版本，实际操作时请以方法名/字段名为准定位。

### 4.1 删除 HLS 专用状态字段

删除 `_VideoPreviewViewState` 中以下字段（约第 63-89 行）：

| 字段 | 行号 | 说明 |
|------|:----:|------|
| `_hlsLocalSeekSlackMs` | 63 | HLS 本地 seek 容差常量 |
| `_hlsSeekJumpThresholdMs` | 64 | HLS seek 跳变检测阈值 |
| `_hlsOriginMs` | 83 | HLS seek-restart 起点偏移 |
| `_hlsSeekInFlight` | 84 | HLS seek 进行中锁 |
| `_applyingHlsTimeline` | 85 | 正在应用 HLS 时间轴标志 |
| `_exoNativeDurationMs` | 87 | Exo 报告的编码前沿时长 |
| `_lastDisplayPosMs` | 89 | 上次显示位置（用于跳变检测） |

**保留** `_scrubbing`（88 行）：进度条拖动标志，简化后仍用于控件自动隐藏控制。

### 4.2 删除 HLS 专用方法

完整删除以下方法（约 250 行）：

| 方法 | 行号 | 职责 |
|------|:----:|------|
| `_captureExoNativeDuration` | 563-579 | 启发式判断编码前沿 |
| `_syncEffectiveDuration` | 583-593 | **篡改 controller.value 覆盖 duration** |
| `_syncHlsOrEffectiveDuration` | 595-601 | HLS 时间轴同步入口 |
| `_syncStreamingTimeline` | 605-634 | stream-local → meta 坐标映射 |
| `_streamingBufferedEndRaw` | 636-641 | 读取缓冲末端 |
| `_streamingFrontierMs` | 645-652 | 计算编码前沿 |
| `_shouldRestartHlsForTarget` | 654-661 | 判断是否需 seek-restart |
| `_handleStreamingScrubEnd` | 663-676 | 拖动结束的 HLS 处理 |
| `_maybeRestartHlsAfterSeekJump` | 678-704 | position 跳变猜 seek |
| `_applyStreamingSeekTarget` | 706-735 | seek 决策（本地 vs restart） |
| `_restartHlsAt` | 737-820 | 杀进程 + 重启 FFmpeg |
| `_videoUrlWithSeekCacheBuster` | 442-451 | HLS seek-restart 的 URL 缓存破坏 |

### 4.3 修改引用了 HLS 逻辑的方法

#### 4.3.1 `_initializePlayer`（约 159-250 行）

删除 HLS 状态重置（约 162-166 行）：

```dart
// 删除以下行：
_hlsOriginMs = 0;
_hlsSeekInFlight = false;
_exoNativeDurationMs = 0;
_scrubbing = false;        // 可保留，下方 _handleSurfaceTap 不依赖
_lastDisplayPosMs = 0;
```

#### 4.3.2 `_tryCreateNetworkController`（约 294-328 行）

删除 HLS formatHint 分支，统一用 progressive（不传 formatHint，让 ExoPlayer 自动识别）：

```dart
// 修改前（约 304-314 行）：
_appendDebugLog(
  '优先尝试在线播放：host=${_safeVideoHost(videoUrl)} '
  'formatHint=${widget.source.strategy == PreviewStrategy.streaming ? 'hls' : 'auto'}',
);
controller = VideoPlayerController.networkUrl(
  Uri.parse(videoUrl),
  httpHeaders: widget.source.headers ?? const <String, String>{},
  formatHint: widget.source.strategy == PreviewStrategy.streaming
      ? VideoFormat.hls
      : null,
);

// 修改后：
_appendDebugLog('优先尝试在线播放：host=${_safeVideoHost(videoUrl)}');
controller = VideoPlayerController.networkUrl(
  Uri.parse(videoUrl),
  httpHeaders: widget.source.headers ?? const <String, String>{},
);
```

#### 4.3.3 `_activateController`（约 330-377 行）

删除时间轴同步调用（约 341 行）：

```dart
// 删除这行：
_syncHlsOrEffectiveDuration(controller);
```

#### 4.3.4 `_handleControllerChanged`（约 523-560 行）

删除三个 HLS 调用（约 547-549 行）：

```dart
// 删除以下三行：
_captureExoNativeDuration(value);
_syncHlsOrEffectiveDuration(controller);
_maybeRestartHlsAfterSeekJump(controller);
```

保留 `_notifyPlaybackState` 与 `_logPlaybackSnapshot`。

#### 4.3.5 `_effectiveDuration`（约 1230-1236 行）

**保留不变**。该方法在 UI 层优先用 `metaMs`（服务端 `ffmpeg -i` 探测的时长）作为兜底，这是合理的——某些容器（如部分 MKV）ExoPlayer 可能读不到准确 duration，metaMs 兜底能保证进度条总时长正确。关键是它**不篡改 `controller.value`**，只在 UI 层覆盖显示，符合本方案原则。

```dart
// 保持原样，无需修改：
Duration _effectiveDuration(VideoPlayerValue value) {
  final metaMs = widget.source.durationMs;
  if (metaMs != null && metaMs > 0) {
    return Duration(milliseconds: metaMs);
  }
  return value.duration;
}
```

#### 4.3.6 进度条 `onDragEnd`（约 1076-1084 行）

删除 streaming 分支，统一走普通逻辑：

```dart
// 修改前：
onDragEnd: () {
  if (widget.source.strategy == PreviewStrategy.streaming) {
    unawaited(_handleStreamingScrubEnd());
  } else {
    _scrubbing = false;
    _restartAutoHideTimer();
  }
},

// 修改后：
onDragEnd: () {
  _scrubbing = false;
  _restartAutoHideTimer();
},
```

### 4.4 清理无用 import

删除 HLS 逻辑后，若以下 import 不再被引用则清理（需实际检查）：

- `import '../../../../app/di/service_locator.dart';` —— 若 `_restartHlsAt` 是唯一引用 `serviceLocator.apiClient` 的地方，可删除；但 `_cacheCoordinator` 也来自 serviceLocator，**通常保留**。

> 提示：用 IDE 的 "Remove unused imports" 功能自动清理。

---

## 五、新增功能实现

> 文件：`d:\tmp\DiuBangNASClient\lib\features\preview\presentation\widgets\video_preview_view.dart`

### 5.1 功能一：倍速播放

#### 5.1.1 新增状态字段

在 `_VideoPreviewViewState` 字段区（约第 88 行 `_scrubbing` 附近）新增：

```dart
/// 当前播放倍速
double _playbackSpeed = 1.0;
/// 长按期间保存的原倍速（松手恢复）
double? _speedBeforeLongPress;
/// 是否正在长按倍速
bool _isLongPressing = false;

/// 可选倍速档位
static const List<double> _speedOptions = [0.5, 1.0, 1.25, 1.5, 2.0];
/// 长按临时倍速
static const double _longPressSpeed = 2.0;
/// 双击快进/快退步长
static const Duration _seekStep = Duration(seconds: 5);
```

#### 5.1.2 新增倍速切换方法

```dart
Future<void> _setPlaybackSpeed(double speed) async {
  final controller = _controller;
  if (controller == null || !_isInitialized) {
    return;
  }
  await controller.setPlaybackSpeed(speed);
  if (!mounted) {
    return;
  }
  setState(() {
    _playbackSpeed = speed;
  });
}
```

#### 5.1.3 初始化时应用倍速

在 `_activateController`（约 376 行 `_appendDebugLog('控制器初始化完成')` 之前）补充：

```dart
await controller.setPlaybackSpeed(_playbackSpeed);
```

保证切换视频后倍速状态延续。

#### 5.1.4 控件层增加倍速按钮

在 `_buildControlsOverlay` 底部工具栏 `Row`（约 1088-1147 行）中，在「缓冲指示」与「时长显示」之间插入倍速按钮：

```dart
// 在 Row children 中，约第 1122 行 Text(_formatDuration(_effectiveDuration...)) 之前插入：
_buildSpeedButton(),
```

新增按钮构建方法：

```dart
Widget _buildSpeedButton() {
  return Padding(
    padding: const EdgeInsets.symmetric(horizontal: 8),
    child: PopupMenuButton<double>(
      tooltip: '播放倍速',
      onSelected: (speed) => unawaited(_setPlaybackSpeed(speed)),
      itemBuilder: (context) => _speedOptions
          .map(
            (speed) => PopupMenuItem<double>(
              value: speed,
              child: Row(
                children: [
                  if (speed == _playbackSpeed)
                    const Icon(Icons.check, size: 18)
                  else
                    const SizedBox(width: 18),
                  const SizedBox(width: 8),
                  Text('${_formatSpeed(speed)}x'),
                ],
              ),
            ),
          )
          .toList(),
      child: Container(
        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
        decoration: BoxDecoration(
          color: Colors.black.withValues(alpha: 0.32),
          borderRadius: BorderRadius.circular(12),
        ),
        child: Text(
          '${_formatSpeed(_playbackSpeed)}x',
          style: const TextStyle(color: Colors.white, fontSize: 12),
        ),
      ),
    ),
  );
}

String _formatSpeed(double speed) {
  if (speed == speed.roundToDouble()) {
    return speed.toStringAsFixed(1);
  }
  return speed.toString();
}
```

### 5.2 功能二：双击前进 5 秒 / 后退 5 秒

#### 5.2.1 改造视频表面手势

修改 `build` 方法中的 `GestureDetector`（约 923-925 行）。保留 `onTap`（切换控件），新增 `onDoubleTapDown`（双击分区 seek）：

```dart
// 修改前：
child: GestureDetector(
  behavior: HitTestBehavior.opaque,
  onTap: _handleSurfaceTap,
  ...
),

// 修改后：
child: GestureDetector(
  behavior: HitTestBehavior.opaque,
  onTap: _handleSurfaceTap,
  onDoubleTapDown: _handleDoubleTapSeek,
  onDoubleTap: () {}, // 必须提供 onDoubleTap 才会触发 onDoubleTapDown
  ...
),
```

#### 5.2.2 新增双击 seek 方法

```dart
void _handleDoubleTapSeek(TapDownDetails details) {
  final controller = _controller;
  if (controller == null || !_isInitialized) {
    return;
  }
  // 根据点击 x 坐标判断左/右半区
  final box = context.findRenderObject() as RenderBox?;
  if (box == null) {
    return;
  }
  final width = box.size.width;
  final isRightHalf = details.localPosition.dx > width / 2;

  final current = controller.value.position;
  final target = isRightHalf
      ? current + _seekStep
      : current - _seekStep;
  unawaited(_seekToClamped(target));
  _showSeekFeedback(forward: isRightHalf);
}

Future<void> _seekToClamped(Duration target) async {
  final controller = _controller;
  if (controller == null || !_isInitialized) {
    return;
  }
  final duration = _effectiveDuration(controller.value);
  final clamped = _normalizePosition(target, duration);
  await controller.seekTo(clamped);
}

/// 双击时显示短暂的方向波纹提示（可选，提升体验）
void _showSeekFeedback({required bool forward}) {
  // 可用 OverlayEntry 或简单的 SnackBar/AnimatedIcon 实现
  // 最简实现：闪烁一次控件层即可，此处留作可选增强
}
```

> **说明**：Flutter 的 `GestureDetector` 内置单击/双击识别——双击时 `onTap` 不会触发（单击会延迟约 300ms 等待是否双击），因此与 `_handleSurfaceTap` 不冲突。ExoPlayer 对 progressive 源的 `seekTo` 会发 HTTP Range 请求，毫秒级响应。

### 5.3 功能三：长按 2 倍速播放

#### 5.3.1 手势层增加长按监听

在 `build` 的 `GestureDetector`（同 5.2.1）继续补充：

```dart
child: GestureDetector(
  behavior: HitTestBehavior.opaque,
  onTap: _handleSurfaceTap,
  onDoubleTapDown: _handleDoubleTapSeek,
  onDoubleTap: () {},
  onLongPressStart: (_) => _beginLongPressSpeed(),
  onLongPressEnd: (_) => _endLongPressSpeed(),
  ...
),
```

#### 5.3.2 新增长按倍速方法

```dart
void _beginLongPressSpeed() {
  final controller = _controller;
  if (controller == null || !_isInitialized || _isLongPressing) {
    return;
  }
  // 已是 2.0x 时不重复保存
  if (_playbackSpeed != _longPressSpeed) {
    _speedBeforeLongPress = _playbackSpeed;
  } else {
    _speedBeforeLongPress = null;
  }
  _isLongPressing = true;
  unawaited(_setPlaybackSpeed(_longPressSpeed));
}

void _endLongPressSpeed() {
  if (!_isLongPressing) {
    return;
  }
  _isLongPressing = false;
  final restore = _speedBeforeLongPress ?? 1.0;
  _speedBeforeLongPress = null;
  unawaited(_setPlaybackSpeed(restore));
}
```

#### 5.3.3 长按倍速提示遮罩

在 `_buildControlsOverlay` 的 `Stack` children 中（约 1014 行附近），增加长按提示。建议放在视频表面层而非控件层，使控件隐藏时也可见：

在 `build` 方法的 `Stack`（约 926 行）中，`_buildControlsOverlay(value)` 之后追加：

```dart
if (_isLongPressing)
  const Positioned(
    top: 24,
    left: 0,
    right: 0,
    child: Center(
      child: _LongPressSpeedBadge(),
    ),
  ),
```

新增 badge 组件：

```dart
class _LongPressSpeedBadge extends StatelessWidget {
  const _LongPressSpeedBadge();

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
      decoration: BoxDecoration(
        color: Colors.black.withValues(alpha: 0.6),
        borderRadius: BorderRadius.circular(20),
      ),
      child: const Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          Icon(Icons.fast_forward_rounded, color: Colors.white, size: 16),
          SizedBox(width: 4),
          Text(
            '2.0x 倍速播放中',
            style: TextStyle(color: Colors.white, fontSize: 13),
          ),
        ],
      ),
    );
  }
}
```

---

## 六、完整文件修改清单

### 6.1 客户端（唯一需改动的项目）

| 目录 | 文件 | 改动类型 | 说明 |
|------|------|----------|------|
| `lib/features/preview/presentation/widgets/` | `video_preview_view.dart` | 修改 | 删除 HLS 逻辑（约 -250 行）+ 新增三功能（约 +150 行） |
| `test/features/preview/presentation/` | `video_preview_view_test.dart`（新增） | 新增 | 倍速/双击/长按的 widget 测试 |

### 6.2 服务端（无需改动）

| 项目 | 改动 |
|------|------|
| `D:\tmp\diubangNASServer_Android` | 无（WebDAV Range 已支持，preview 默认 progressive） |
| `D:\tmp\diubangNASServer_Windows` | 无（同上） |

---

## 七、测试计划

### 7.1 单元/Widget 测试

新增 `test/features/preview/presentation/video_preview_view_test.dart`：

- **倍速切换**：点击倍速按钮选择 1.5x，验证 `controller.setPlaybackSpeed(1.5)` 被调用，UI 显示 "1.5x"。
- **倍速延续**：切换视频后，新 controller 应用上次的 `_playbackSpeed`。
- **双击前进**：在画面右半区双击，验证 position 增加 5 秒。
- **双击后退**：在画面左半区双击，验证 position 减少 5 秒且不低于 0。
- **长按倍速**：`onLongPressStart` 后倍速变 2.0x，`onLongPressEnd` 后恢复原速。
- **长按恢复原速**：长按前若为 1.25x，松手后恢复 1.25x（非 1.0x）。
- **单击仍切换控件**：单击不触发 seek，控件显隐切换正常。

### 7.2 手动验证清单

| 场景 | 预期 |
|------|------|
| MP4 视频预览 | duration 准确，进度条可拖动，毫秒级响应 |
| 拖动进度条到任意位置 | 无卡顿，无"定位播放失败"错误 |
| 双击右半屏 | 前进 5 秒 |
| 双击左半屏 | 后退 5 秒 |
| 单击画面 | 切换控件显隐（不被双击劫持） |
| 长按画面 | 切换 2.0x，显示"2.0x 倍速播放中" |
| 松手 | 恢复原速，提示消失 |
| 倍速按钮 | 弹出 0.5/1.0/1.25/1.5/2.0 档位 |
| 选择 1.5x | 播放加速，按钮显示 1.5x |
| 连接 Windows 服务端 | 行为一致 |
| 连接安卓服务端 | 行为一致 |

### 7.3 回归测试

- 确认 native 模式（本地缓存播放）不受影响：`_createVideoController` 的非网络分支未改动。
- 确认封面图、全屏、下载等功能不受影响。
- 确认 `gallery_page.dart` 调用 `VideoPreviewView` 的接口未变（widget 参数无变化）。

---

## 八、风险与回滚

### 8.1 风险评估

| 风险 | 概率 | 影响 | 应对 |
|------|:----:|:----:|------|
| 单击/双击 300ms 延迟影响切换控件体感 | 中 | 低 | Flutter 标准行为，主流视频 App 均如此；如不可接受可改用 `RawGestureDetector` 自定义识别 |
| 某些 MKV 容器 ExoPlayer 读不到 duration | 中 | 中 | `_effectiveDuration` 保留 metaMs 兜底（UI 层覆盖，不碰平台层） |
| 非标准 MP4（moov atom 在文件尾）首次 seek 慢 | 低 | 低 | ExoPlayer 会先请求文件尾读取 moov，首次 seek 可能多一次 Range 请求，后续正常 |
| 安卓服务端若手动开启 HLS（非默认配置）则 seek 仍 404 | 低 | 中 | 本方案不启用 HLS；如需启用见附录对齐安卓服务端 |

### 8.2 回滚方案

- 所有改动集中在单个文件 `video_preview_view.dart`（+1 测试文件）。
- 回滚方式：`git revert` 对应提交，或 `git checkout` 恢复该文件。
- 无服务端改动、无原生改动、无依赖变更，回滚零风险扩散。

---

## 九、实施顺序建议

建议按以下顺序分步实施，每步可独立验证：

1. **删除 HLS 补偿逻辑**（第四章）→ 验证 progressive 播放正常、进度条拖动流畅、duration 准确
2. **实现倍速播放**（第 5.1 节）→ 验证档位切换
3. **实现双击前进 5 秒**（第 5.2 节）→ 验证左右分区 seek
4. **实现长按 2 倍速**（第 5.3 节）→ 验证临时倍速与恢复
5. **补充测试**（第七章）→ 验证自动化覆盖
6. **全量手动回归**（第 7.2 节）→ 连接两个服务端各测一轮

---

## 附录 A：HLS 后备方案（可选，非本次必需）

若未来需要对 ExoPlayer 不支持的格式（如 HEVC/MKV 复杂编码）启用 HLS 转码后备：

### A.1 安卓服务端需对齐 Windows 版本

将以下 Windows 服务端文件的内容同步到安卓服务端对应位置：

| Windows 文件 | 安卓目标文件 | 关键差异 |
|------|------|------|
| `lib/features/api/services/video_hls_session_service.dart` | 同路径 | 新增 `seekSession` + `-ss` + `temp_file` + `_isCompleteAsset` |
| `lib/features/api/handlers/preview_hls_handler.dart` | 同路径 | 新增 `seek` handler |
| `lib/features/api/handlers/api_router.dart` | 同路径 | 注册 `POST /preview/hls/seek` |
| `lib/core/storage/ffmpeg_hls_encoder.dart` | 同路径（新增） | 编码器选择逻辑 |

### A.2 客户端 HLS seek 简化方案

启用 HLS 后备时，客户端**不应恢复**之前的"篡改 controller.value + seek-restart"复杂逻辑，而应采用简化方案：

- HLS VOD 模式（带 `#EXT-X-ENDLIST`）：ExoPlayer 原生报告准确 duration，无需任何补偿。
- 若用 EVENT + seek-restart：seek 时调用 `POST /preview/hls/seek?path=...&tMs=...`，服务端用 `-ss` 重启 FFmpeg，客户端 `dispose` 旧 controller 后用新 URL 重建即可，**不篡改 controller.value**。

---

*本指南基于 2026-08-07 三个项目的代码状态编写，实际行号可能随版本演进偏移，请以方法名/字段名为准定位。*
